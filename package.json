{
  "name": "tree-sitter-bsv",
  "version": "0.1.0",
  "description": "Bluespec SystemVerilog grammar for tree-sitter",
  "main": "bindings/node",
  "keywords": [
    "tree-sitter",
    "parser",
    "bluespec",
    "bsv",
    "hardware"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "node-addon-api": "^7.0.0",
    "node-gyp-build": "^4.5.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.0"
  },
  "scripts": {
    "build": "tree-sitter generate && node-gyp rebuild",
    "test": "tree-sitter test"
  },
  "tree-sitter": [
    {
      "scope": "source.bsv",
      "file-types": [
        "bsv",
        "bs"
      ]
    }
  ]
}
